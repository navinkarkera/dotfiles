session: sprintcraft

root: ~/work/opencraft/sprintcraft/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  COMPOSE_FILE: local.yml
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py310/

windows:
  - name: code
    commands:
      - activate
      - e .
  - name: shell
    commands:
      - activate
