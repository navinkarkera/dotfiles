session: ytp_api

root: ~/work/master-devstack/src/ytp_api/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py38/
  NVIM_SERVER: /tmp/ytp_api-nvim.pipe
  PUEUE_GROUP: ytp_api
  GIT_PARENT_BRANCH: opencraft-release/maple.3

windows:
  - name: code
    layout: main-horizontal
    commands:
      - activate
      - e
    panes:
      - type: vertical
        commands:
          - activate
  - name: shell
    commands:
      - activate
