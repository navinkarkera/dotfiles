session: taxonomy_connector

root: ~/work/master-devstack/src/taxonomy-connector/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py38/
  NVIM_SERVER: /tmp/taxonomy_connector-nvim.pipe
  GIT_PARENT_BRANCH: openedx-master

windows:
  - name: code
    layout: main-horizontal
    commands:
      - activate
      - e
    panes:
      - type: vertical
        commands:
          - activate
  - name: shell
    commands:
      - activate
