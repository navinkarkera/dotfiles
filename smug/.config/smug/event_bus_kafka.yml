session: event_bus_kafka

root: ~/work/master-devstack/src/event-bus-kafka/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py38/
  NVIM_SERVER: /tmp/event_bus_kafka-nvim.pipe
  GIT_PARENT_BRANCH: origin/main
  PUEUE_GROUP: event_bus_kafka

windows:
  - name: code
    commands:
      - activate
      - e
  - name: shell
    commands:
      - activate
