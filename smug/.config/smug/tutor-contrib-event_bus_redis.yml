session: tutor-contrib-event_bus_redis

root: ~/work/opencraft/tutor-contrib-event-bus-redis/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py38/
  NVIM_SERVER: /tmp/tutor-contrib-event_bus_redis-nvim.pipe
  GIT_PARENT_BRANCH: upstream/main
  PUEUE_GROUP: tutor_contrib_event_bus_redis

windows:
  - name: code
    commands:
      - activate
      - e
  - name: shell
    commands:
      - activate
