session: ansible-secrets

root: ~/work/ansible-secrets/

before_start:
  - git config user.email 'navin@opencraft.com'

env:
  PYTHON_ENV_PATH: /home/navin/work/python_shared_venvs/py36

windows:
  - name: code
    commands:
      - activate
      - e
  - name: shell
    commands:
      - activate
