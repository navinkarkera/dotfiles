#!/usr/bin/env bash

case "$(printf "kill\nlogout\nreboot\nshutdown" | fuzzel --dmenu 4)" in
	kill) ps -u $USER -o pid,comm,%cpu,%mem | fuzzel --dmenu -p Kill: | awk '{print $1}' | xargs -r kill ;;
	logout) loginctl kill-session $XDG_SESSION_ID ;;
	reboot) systemctl reboot -i ;;
	shutdown) shutdown now ;;
	*) exit 1 ;;
esac
