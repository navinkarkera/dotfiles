#!/usr/bin/env bash

# ffmpeg editing process for my recordings: combining a screen recording and a separate audio recording

# Map audio to video
ffmpeg -i video*.mkv -i audio*.wav -map 0 -map 1:a -c:v copy -shortest final.mkv

notify-send "Video edit finished"
